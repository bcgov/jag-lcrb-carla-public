#!/bin/sh
if ! whoami &> /dev/null; then
  if [ -w /etc/passwd ]; then
    echo "${USER_NAME:-splunk}:x:$(id -u):0:${USER_NAME:-splunk} user:${HOME}:/sbin/nologin" >> /etc/passwd
  fi
fi
exec "/sbin/entrypoint.sh $@" 