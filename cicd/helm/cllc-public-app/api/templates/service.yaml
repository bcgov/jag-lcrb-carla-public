apiVersion: v1
kind: Service
metadata:
  name: {{ include "cllc-api.fullname" . }}
spec:
  selector: {{- include "cllc-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: 8080
      targetPort: http
