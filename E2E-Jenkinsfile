node('nodejs') {
	stage('FT on Dev') {
		//the checkout is mandatory, otherwise functional test would fail
        echo "checking out source"
        echo "Build: ${BUILD_ID}"
        checkout scm
        dir('cllc-public-app/ClientApp') {
	    try {
				sh 'npm install'
                sh './node_modules/@angular-cli/bin/ng e2e --no-serve --base-href=http://lclb-cllc-dev.pathfinder.gov.bc.ca'
	    } finally {
		        archiveArtifacts allowEmptyArchive: true, artifacts: 'e2e-reports/**/*'                
                junit 'junitresults.xml'
                publishHTML (target: [
                            allowMissing: false,
                            alwaysLinkToLastBuild: false,
                            keepAll: true,
                            reportDir: 'e2e-reports',
                            reportFiles: 'chrome-test-report.html',
                            reportName: "E2E Report"
                        ])                    
	    }
        }
    }
}

